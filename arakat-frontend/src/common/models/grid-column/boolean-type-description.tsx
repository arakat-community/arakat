/**
 * define messages for true/false values which will be shown on grid rows according to value of column
 * this messages will be also used for filtering column
 */
export interface IGridColumnBooleanTypeDescription {
    enabledMessage: string;
    disabledMessage: string;
}