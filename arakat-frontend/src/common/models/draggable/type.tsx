/**
 * defines draggable types, according to that types, you can control which component could be dropped on droppable source
 */
export enum DraggableType {
    Visualization = "Visualization",
    Filter = "Filter",
    DataSourceItem = "DataSourceItem",
}
