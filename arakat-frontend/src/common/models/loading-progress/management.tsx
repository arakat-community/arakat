/**
 * when auto, a linear progress appears on appbar
 * when manuel, it is waited to be managed by developer by listening redux state changes
 */
export enum LoadingProgressManagement {
    auto,
    manuel,
}
