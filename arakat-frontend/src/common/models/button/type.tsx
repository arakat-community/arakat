/**
 * type of buttons. each type changes button's
 * color to emphise the importance of action to user
 */
enum ButtonType {
    normal,
    action,
    warning,
}

export default ButtonType;
