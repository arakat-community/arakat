import React from "react";
import PageViewComponent from "../../components/page-view";

const PageViewContainer: (props: any) => JSX.Element = (props) => ( <PageViewComponent {...props}/>);

export default PageViewContainer;
