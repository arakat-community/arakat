import React from "react";
import PageHeaderComponent from "../../components/page-header";

const PageHeaderContainer: (props: any) => JSX.Element = (props) => ( <PageHeaderComponent {...props}/>);

export default PageHeaderContainer;
