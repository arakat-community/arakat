import React from "react";
import UserInfoViewComponent from "../../components/user-info-view";

const UserInfoViewContainer: (props: any) => JSX.Element = (props) => ( <UserInfoViewComponent {...props}/>);

export default UserInfoViewContainer;
