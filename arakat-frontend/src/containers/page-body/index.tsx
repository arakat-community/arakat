import React from "react";
import PageBodyComponent from "../../components/page-body";

const PageBodyContainer: (props: any) => JSX.Element = (props) => ( <PageBodyComponent {...props}/>);

export default PageBodyContainer;
