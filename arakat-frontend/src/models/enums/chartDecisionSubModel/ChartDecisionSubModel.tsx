
export enum ComparisonSubModel {
    AMONG_ITEMS = "comparison among items",
    OVER_TIME = "comparison over time",
}

export enum RelationshipSubModel {
    TWO_VARIABLES = "two variables",
    THREE_VARIABLES = "three variables",
}

export enum DistributionSubModel {
    SINGLE_VARIABLE = "single variable",
    TWO_VARIABLES = "two variables",
    THREE_VARIABLES = "three variables",
}

export enum CompositionSubModel {
    CHANGING_OVER_TIME = "changing over time",
    STATIC = "static",
}

export enum ComparisonOverTimeDecisionType {
    MANY_PERIODS = "many periods",
    FEW_PERIODS = "few periods"
}